import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nosiah.discipline',
  appName: 'DISCIPLINE.',
  webDir: 'dist',
  backgroundColor: '#0a0a12',
};

export default config;
